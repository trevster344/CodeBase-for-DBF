{
  "name": "codebase-node-tests",
  "version": "0.1.0",
  "description": "Node.js / TypeScript console tests for the CodeBase FFI bindings (interfaces/Node)",
  "private": true,
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --no-warnings test.ts",
    "test:x64": "node --experimental-strip-types --no-warnings test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
